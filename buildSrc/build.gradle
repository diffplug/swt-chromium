apply from: '../gradle/loadProps.gradle'

repositories {
	mavenCentral()
}

apply plugin: 'java'
apply plugin: 'eclipse'

dependencies {
	compile "com.diffplug.gradle:goomph:${VER_GOOMPH}"
	compile "de.undercouch:gradle-download-task:${VER_DOWNLOAD}"
}
